<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Ticket - SeyTic</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body class="bg-gradient-to-b from-slate-50 to-white min-h-screen">

<!-- Navbar -->
<nav class="bg-white border-b border-slate-200">
  <div class="max-w-[1440px] mx-auto px-6 py-4 flex justify-between items-center">
    <div class="flex items-center space-x-2">
      <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center">
        <i data-lucide="ticket" class="text-white w-5 h-5"></i>
      </div>
      <span class="text-slate-900 font-semibold">SeyTic</span>
    </div>

    <div class="flex items-center gap-6">
      <a href="/dashboard.php" class="text-slate-700 hover:text-slate-900">Dashboard</a>
      <a href="/tickets.php" class="text-blue-600 font-medium">Tickets</a>
      <a href="/logout.php" class="px-3 py-1 border border-slate-300 text-slate-700 rounded-lg hover:bg-slate-100">
        Logout
      </a>
    </div>
  </div>
</nav>

<!-- Page Container -->
<div class="max-w-[1440px] mx-auto px-6 py-12">

  <h1 class="text-slate-900 text-3xl font-semibold mb-8">Create a Ticket</h1>

  {% if error %}
    <div class="mb-6 p-3 bg-red-100 text-red-700 rounded-lg">
      {{ error }}
    </div>
  {% endif %}

  {% if success %}
    <div class="mb-6 p-3 bg-green-100 text-green-700 rounded-lg">
      {{ success }}
    </div>
  {% endif %}

  <form method="post" action="/tickets.php" class="grid gap-6 max-w-2xl">

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Title *</label>
      <input type="text" name="title" class="w-full border border-slate-300 rounded-lg px-4 py-2"
             placeholder="Short title" required>
    </div>

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Status *</label>
      <select name="status" class="w-full border border-slate-300 rounded-lg px-4 py-2" required>
        <option value="open">Open</option>
        <option value="in_progress">In Progress</option>
        <option value="closed">Closed</option>
      </select>
    </div>

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Description</label>
      <textarea name="description" rows="5" class="w-full border border-slate-300 rounded-lg px-4 py-2"
                placeholder="Describe the issue (optional)"></textarea>
    </div>

    <button class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg">
      Create Ticket
    </button>
  </form>
</div>

<script>lucide.createIcons()</script>
</body>
</html>
