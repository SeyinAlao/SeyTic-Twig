<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Ticket - SeyTic</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/lucide@latest"></script>
</head>

<body class="bg-gradient-to-b from-slate-50 to-white min-h-screen">

<div class="max-w-[700px] mx-auto px-6 py-12">

  <h1 class="text-3xl text-slate-900 font-semibold mb-8">Edit Ticket</h1>

  <form method="post" class="grid gap-6">

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Title</label>
      <input type="text" name="title" value="{{ ticket.title }}" class="w-full border border-slate-300 rounded-lg px-4 py-2">
    </div>

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Status</label>
      <select name="status" class="w-full border border-slate-300 rounded-lg px-4 py-2">
        <option value="open" {% if ticket.status == 'open' %}selected{% endif %}>Open</option>
        <option value="in_progress" {% if ticket.status == 'in_progress' %}selected{% endif %}>In Progress</option>
        <option value="closed" {% if ticket.status == 'closed' %}selected{% endif %}>Closed</option>
      </select>
    </div>

    <div>
      <label class="block mb-2 text-slate-700 font-medium">Description</label>
      <textarea name="description" rows="5" class="w-full border border-slate-300 rounded-lg px-4 py-2">{{ ticket.description }}</textarea>
    </div>

    <button class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg">Save Changes</button>
  </form>

  <div class="mt-6">
    <a href="/tickets.php" class="text-slate-600 hover:underline">← Back to Tickets</a>
  </div>

</div>

<script>lucide.createIcons()</script>
</body>
</html>
